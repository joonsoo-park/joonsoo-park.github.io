- clickup 코드가 계속 깨져서 부득이하게 별도로 작성합니다
-
- # 이미지 다운로드
- ```bash
  http://nexus.exem.io/repository/package-exemone/images/exemone-sparselog/exemone-sparselog-v3.0.604.0.tar.gz
  ```
-
- # 이미지 로드
- ```bash
  docker load -i exemone-sparselog-v3.0.604.0.tar.gz
  ```
-
- # Dockerfile 추가
- `${EXEMONE_HOME}/dockerfile/exemone-sparselog/Dockerfile`
- ```bash
  mkdir ${EXEMONE_HOME}/dockerfile/exemone-sparselog
  
  cat > Dockerfile <<'EOF'
  ARG SPARSELOG_IMAGE
  ARG SPARSELOG_VERSION
  FROM ${SPARSELOG_IMAGE}:${SPARSELOG_VERSION}
  
  ARG EXEMONE_USER_UID
  ARG EXEMONE_USER_GID
  
  USER root
  
  # 설치 환경에 따른 uid, gid 변경
  RUN usermod -u ${EXEMONE_USER_UID} exemone && \
      groupmod -g ${EXEMONE_USER_GID} exemone
  
  RUN chown -R exemone:exemone /exemone-sparselog
  
  USER exemone
  EOF
  ```
-
- # .env 파일 sparselog 추가
- ```env
  SPARSELOG_IMAGE=maxgauge/exemone-sparselog
  SPARSELOG_VERSION=v3.0.604.0
  ```
-
- # exemone-sparselog 컨테이너 디렉토리 추가
- ### 필요 디렉토리 추가
- ```bash
  mkdir ${EXEMONE_HOME}/containers/exemone-sparselog
  
  mkdir ${EXEMONE_HOME}/containers/exemone-sparselog/log
  mkdir ${EXEMONE_HOME}/containers/exemone-sparselog/model
  ```
- ### .env 파일 추가
- ```env
  # ======================== Redis 설정 =========================
  REDIS_HOST=redis # 레디스 컨테이너
  REDIS_PORT=6379
  REDIS_DB=1
  # REDIS_PASSWORD=your_redis_password
  
  # ======================== Kafka 설정 =========================
  KAFKA_BOOTSTRAP_SERVERS=kafka:9092 # 카프카 컨테이너
  KAFKA_GROUP_ID=log-analyzer
  KAFKA_AUTO_OFFSET_RESET=latest
  KAFKA_SESSION_TIMEOUT_MS=10000
  KAFKA_ENABLE_AUTO_COMMIT=false
  KAFKA_FETCH_MIN_BYTES=1048576
  KAFKA_MAX_POLL_INTERVAL_MS=300000
  
  # ======================== 애플리케이션 설정 =========================
  AUTO_START_ENABLED=true
  AUTO_START_TOPICS=unified.log.LogBatchV2
  
  # ======================== 서버 설정 =========================
  # Celery 설정
  CELERY_CONCURRENCY=2
  CELERY_LOG_LEVEL=info
  
  # 로그 디렉토리 설정
  LOG_DIR=/exemone-sparselog/log
  
  # ======================== Python 설정 =========================
  PYTHONPATH=/exemone-sparselog
  ```
-
- #